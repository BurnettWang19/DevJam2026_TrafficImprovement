# 這個專案真正解決了什麼問題

> 一句話：**它把「路口設計好不好」這件事，從少數工程師的現場專業判斷，變成任何人、任何座標、90 秒就能取得的一份有證據、有評分、有改善圖的診斷書。**

---

## 一、台灣的真實處境

台灣的道路問題不是「駕駛不守規矩」這麼簡單。國際媒體用「行人地獄」形容台灣街道之後，
討論大多繞在執法與宣導上，但真正的病灶在**實體設計**：

- 路口轉角是一個大圓弧，右轉車根本不需要減速。
- 斑馬線緊貼路口，右轉車在「轉彎當中」才遇到行人，那正是視線死角。
- 人行道在路口斷掉，行人被迫走進車道，然後被責怪「不走人行道」。
- 路口鋪面一大片沒有標線，人車各自猜對方要往哪走。
- 進口道與出口道共線，路口變成一條可以直接加速穿越的大馬路。

這些**全部是可以在設計圖上改掉的東西**，不是靠罰單。
而且它們有明確的國際解法：荷蘭式受保護路口、轉角外推（bulb-out）、
穿越線退縮一個車身、小轉彎半徑、行人庇護島、Zone 30、Vision Zero、Safe System。

**問題不是不知道怎麼改，而是沒有人有能力把全台灣幾萬個路口一個一個看過去，並說出「這個路口錯在哪、該怎麼改」。**

> 具體傷亡數字請引用交通部道安資訊查詢網的官方統計；本文件刻意不寫未經查證的數字，
> 因為這個系統的立場就是「每個結論都要有證據來源」。

---

## 二、五個真正的痛點

### 痛點 1｜民眾看得到危險，但講不出技術語言

一般人每天經過那個路口，知道它很危險。但陳情信只能寫「這裡很危險，請改善」。
沒有座標、沒有指標、沒有違反哪一條原則、沒有替代設計 —— 這種陳情**在行政流程裡沒有重量**，
最後回覆通常是「已加強巡查」或「將納入未來評估」。

**這個系統給的是**：C1–C8 八項評分、每一項的分數與信心度、問題的方位（「東北轉角」「北側支道」）、
證據等級（OBSERVED / MEASURED / INFERRED）、嚴重度（LOW→CRITICAL），
以及一張**可以指著講的改善設計圖**。從「我覺得危險」變成「北側穿越線距轉角不足 1 公尺，
建議退縮 5 公尺並設轉角庇護島」。

### 痛點 2｜專業診斷太貴、太慢，所以永遠只能抽樣

路口會勘要交通工程師到現場、量幾何、看車流、開會。一次一個路口。
全台灣的路口數量是幾萬個等級 —— 用這個方式**在數學上就不可能普查**。
結果是：只有出事的路口、有民代關注的路口、有預算的路口會被檢視。

**這個系統改變的是成本結構**：資料來源是 OpenStreetMap（免費）＋ 衛星影像
（Google Static Maps，或免金鑰的 Esri World Imagery 備援），分析是七個 Gemini 代理人角色。
一個路口約 90 秒、成本是幾次 API 呼叫。
**這讓「掃過整個行政區的每一個路口，然後照嚴重度排序」第一次變成可執行的事。**

### 痛點 3｜改善總是發生在事故之後

現行邏輯是被動的：等事故統計累積出「肇事熱點」，才啟動改善。
代價已經先付掉了，而且付的是人命。

**這個系統是事前的**：它看的是**幾何本身**——轉彎半徑、穿越距離、退縮、視距、
速度相容性、衝突點數量——這些是事故的**成因**，不必等結果出現。
評分標準寫得很清楚：`Evaluate conflict exposure, not accident certainty`。
它不預測會不會出事，它指出這個設計把人放在多大的風險曝露裡。

### 痛點 4｜標準存在，但沒有系統性落地

Safe System、Vision Zero、日本 Zone 30 Plus、歐洲 Sustainable Safety、
自我解釋道路（self-explaining road）—— 這些原則都是公開的。
但一份原則文件不會自己去檢查第 37 個路口有沒有照做。

**這個系統把標準變成可執行的檢核器**：
`backend/prompts/00_scorer_system_prompt.md` 就是評分標準本體，
後端**每次呼叫模型都重新從磁碟讀取**，改完存檔下一次分析立刻生效，不必改程式、不必重啟。

這件事的意義比看起來大：
- 各縣市可以放各自的設計規範。
- 學校可以放教學版標準做教材。
- 倡議團體可以放更嚴格的行人優先標準。
- 標準被改了，**每一個路口的評分會一致地跟著改** —— 這是人力審查做不到的一致性。

### 痛點 5｜工程語言與民眾語言之間沒有橋

工程單位產出的是 CAD 圖與規範條號，民眾看不懂，所以無法參與。
民眾產出的是抱怨與影片，工程單位無法直接拿去施工，所以無法採用。
兩邊都在講話，但沒有共用的物件。

**這個系統的輸出刻意做成雙向可讀**：
- 三個子代理人（斑馬線／人行道／車道標線）產出的每個問題，
  `title` 限 20 字、`evidence` 限 60 字、`solution` 限 40 字**且必須是可施工的具體做法**
  —— prompt 裡明文禁止寫「應加強宣導」這類空話。
- 改善設計圖上有 3–5 個白話編號標記：「轉角外推」「斑馬線退縮5m」「車道偏心1.5m」。
- 報告代理人被要求講**後果**而不是術語：
  ✗「斑馬線未退縮」　✓「斑馬線貼著路口，右轉車看不到行人」。

同一份輸出，民眾看得懂，工程師也拿得到數值。

---

## 三、系統實際做了什麼（十個步驟，各自對應哪個痛點）

```
使用者輸入 (lat, lng, 邊長 40~400 m)
  │
  ① OpenStreetMap Overpass ── 人行道／斑馬線／道路／緣石（現成向量）
  ② 衛星影像              ── Google Static Maps 或 Esri（免金鑰備援）
  ③ Gemini 視覺辨識        ── 補上 OSM 沒有的車道線／停止線／槽化線   ← 痛點 2：資料自己生
  ④ 合併向量 → 現況向量圖
  ⑤ 評分代理人 C1–C8       ── 標準來自外部檔案，熱讀取                ← 痛點 4：標準可換
  │    └ 無重大問題 → verdict: no_problem
  ⑥ 路口型態判斷           ── 非路口就中斷                            ← 不照單全收
  ⑦ 三個子代理人平行找問題  ── 斑馬線／人行道／車道標線 → 存入記憶體    ← 痛點 1：問題具體化
  ⑧ 重繪符合標準的設計向量圖 ── 退縮／外推／小半徑／偏心／庇護島        ← 痛點 5：給出替代方案
  ⑨ 產圖                   ── 設計圖＋白話標註圖＋（選用）AI 擬真圖
  ⑩ 讀回記憶體＋比對經典案例 ── 荷蘭受保護路口／英國圓環／日本行人時相／NACTO 外推
                            → 條列式改善說明                          ← 痛點 3、5
```

### 四個特別關鍵的設計決定

**1. 「非路口」會主動中斷。**
點到一段路段而不是路口時，系統會回 `not_intersection` 並停止，不會硬生出一份分析。
這是可信度的基礎 —— 一個什麼都能給你答案的工具，等於什麼答案都不能信。

**2. 反幻覺規則寫進評分標準本體。**
`Never assume that infrastructure exists if it cannot be observed`、
證據不足時給 `null` 而不是 `0`、
`Do not convert missing information into a negative safety finding`、
有效證據少於 4 項時 `overall_score` 直接給 `null` 並列出 `missing_data`。
**這讓這份診斷書可以被反駁** —— 而可被反駁，才是它能進入公共討論的資格。
系統也明確告知模型手上沒有什麼：街景、TDX、車流量、速率、號誌時相、事故史全部列在 `unavailable_data`。

**3. 前後對照，而不只是打分數。**
只給分數的工具會停在「所以呢？」。這個系統一定產出**改善後的設計向量圖**，
並用 `addresses` 欄位把每一個改動綁回它解決的那個問題編號（`sw-1`、`cw-2`、`lm-1`）。
問題 → 改動 → 效果，一條一條對得起來。

**4. 經典案例庫是「可以抄的解答」而不是裝飾。**
`example/index.json` 裡每個案例都寫了 `problems_addressed`（它解決了哪些問題）
與 `why_it_works`（為什麼有效，帶數字：緣石半徑縮到 5~7 公尺、轉彎車速壓到 15 km/h 以下）。
系統依路口型態與問題關鍵字比對，讓輸出從「你這裡錯了」變成「別的國家怎麼做對的」。

---

## 四、誰會真的用它

| 使用者 | 用它做什麼 |
|---|---|
| 一般民眾 / 家長 | 把自家孩子上學經過的路口跑一次，拿一份帶座標、帶指標、帶設計圖的陳情附件 |
| 里長 / 民意代表 | 質詢或爭取預算時，把「這裡很危險」升級成八項評分與替代設計 |
| 交通局 / 工程單位 | **批次掃描全區路口並依嚴重度排序**，把有限的會勘與預算資源放在最該去的地方 |
| 交通倡議團體 | 用一致的標準大量產出案例，讓倡議從個案變成結構性證據 |
| 學校 / 教學 | 讓學生直接看到「同一個路口，設計改了會長什麼樣」 |
| 記者 | 報導路口事故時，附上該路口的設計診斷，把敘事從「駕駛疏失」拉回「設計缺陷」 |

真正的槓桿在**第三列**。單點查詢是 demo；**掃描整座城市然後排序**才是這套架構真正解鎖的東西 ——
因為它的邊際成本已經低到可以這樣做。

---

## 五、它「不」解決什麼（誠實的邊界）

這一節同樣重要。誇大能力會讓前面所有的可信度歸零。

- **不是可施工的圖。** 重繪代理人輸出的是折線示意圖，不是 CAD 施工圖、不含標準圖說與工程數量。
- **看不到號誌時相、車流量、速率、事故史。** 這些資料目前沒有接入（TDX 是明確的下一步），
  所以號誌設計相關的問題它無法評估，而且它會**主動說自己不知道**。
- **看不到路面以外的東西。** 只有正射衛星影像，沒有街景，所以電桿、變電箱、招牌造成的
  實際淨寬與視距遮蔽只能推論，證據等級是 `INFERRED`。
- **受影像解析度限制。** 單張影像上限決定了邊長超過約 400 公尺就看不清標線，前端滑桿因此鎖在 40–400 m。
  Esri 各地供圖層級不同（台灣都市約 z19≈0.27 m/px、台灣鄉間 z18）。
- **受 OSM 資料完整度限制。** 台灣的人行道與斑馬線在 OSM 上的覆蓋不均，
  所以第 ③ 步用視覺辨識補；補不到的部分會誠實標為證據不足，而不是判定「沒有人行道」。
- **不能取代現場會勘、法規審查與專業技師簽證。** 它的定位是**大規模初篩與溝通工具**，
  把人的專業時間留給真正需要判斷的路口。
- **AI 判讀會錯。** 因此每個結論都帶 `confidence`，且系統設計成可被驗證與反駁。

---

## 六、為什麼這個方案有機會成立

不是因為用了 AI，而是因為**它把一件原本受人力上限限制的事，換成了受 API 額度限制的事**。

|  | 現行方式 | 這個系統 |
|---|---|---|
| 一個路口的診斷 | 工程師到場、量測、開會（天／週） | 約 90 秒（有快取時 0.3 秒） |
| 涵蓋範圍 | 抽樣：出事的、有民代的、有預算的 | 原則上可以掃全區 |
| 標準一致性 | 依審查者的經驗與立場 | 同一份 system prompt，所有路口一致 |
| 換標準的代價 | 重新訓練、重新開會 | 改一個 md 檔存檔，下一次分析生效 |
| 民眾能否參與 | 只能陳情文字 | 自己跑，拿到帶證據的診斷書 |
| 產出 | 會勘紀錄 | 評分＋問題清單＋改善設計圖＋國際案例對照 |

---

## 七、從 demo 到真正解決問題，下一步是什麼

1. **批次化。** 目前是單點查詢。接一個「輸入行政區 → 自動列舉所有路口 → 全部分析 → 依嚴重度排序」
   的批次模式，這是從工具變成治理基礎設施的關鍵一步。
2. **接入 TDX 與事故資料。** 讓評分從「幾何風險」升級成「幾何風險 × 實際暴露量」，
   排序才會真的貼近該優先改哪裡。評分標準的 Evidence Rules 已經預留了 TDX 這一項。
3. **標準本地化。** 把 `00_scorer_system_prompt.md` 換成／並列台灣現行的道路設計規範與各縣市標準，
   讓輸出可以直接對上主管機關認得的條號。
4. **接到真實的陳情管道。** 讓一次分析可以一鍵產生一份格式正確、可送出的改善建議書。
5. **可稽核性。** 每份診斷保留輸入影像、向量資料、模型版本與 prompt 指紋
   （快取指紋機制已經在做這件事），讓結論在數月後仍可重現與檢驗。
6. **街景與實地校正。** 補上視距與人行道有效淨寬這兩個目前只能推論的維度。

---

## 結語

台灣的道路問題不缺人抱怨，也不缺國際上早已驗證過的解法。
缺的是**一個能大規模、一致地、用證據把「這個路口錯在哪、該怎麼改」講清楚的東西**。

那個東西，就是這個專案在做的事。
