# Classic cases

`cases.json` stores normalized reference cases. Add only sources and images that the project is
licensed to display or send to a model. Keep source attribution and applicability limitations with
every case.
